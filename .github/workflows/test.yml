name: CI tests

on:
  pull_request:
    branches: ["main"]

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      
      - name: Run all tests
        uses: ./.github/actions/run-all-tests
        with:
          node-version: '24.x'
